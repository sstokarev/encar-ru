{
  "name": "encar-ru",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --outfile=site/widget.js --minify",
    "build:bookmarklet": "node scripts/build-bookmarklet.mjs",
    "build:extension": "node scripts/build-extension.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "jsdom": "^24.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
